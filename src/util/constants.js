
export const DEFAULT_PAGINATION = {
    pageNum: 1,
    pageSize: 10,
    total: 0
};

export const DEFAULT_LIST = {
    data: [],
    pagination: DEFAULT_PAGINATION
};

export default {
    DEFAULT_PAGINATION,
    DEFAULT_LIST
};
